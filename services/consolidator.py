"""
services/consolidator.py — Etapa 4: Consolidación de datos (PLACEHOLDER).
"""

import logging

logger = logging.getLogger(__name__)


async def consolidar(cliente_id: str, **datos) -> dict:
    logger.info("PLACEHOLDER Etapa 4 — consolidar pendiente de implementar")
    # TODO: combinar datos_form + analisis_transcript + analisis_competidores + keyword_research
    return {}
