"""
services/methodology.py — Etapa 5: Generación de metodología (PLACEHOLDER).
"""

import logging

logger = logging.getLogger(__name__)


async def generar_metodologia(cliente_id: str, contexto: dict) -> str:
    logger.info("PLACEHOLDER Etapa 5 — generar_metodologia pendiente de implementar")
    # TODO: usar AIProvider para generar las 9 secciones del documento
    return ""
